export interface WinnerModel {
  userId: number,
  eventHsvId: number,
  blacklistId: number,
  name: string,
  email: string,
  escortName: string,
  winner: boolean,
  substituteWinner: boolean,
  blacklistCounter: number,
}
